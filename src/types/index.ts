export interface NavItem {
    name: string;
    href: string;
}

export interface Service {
    title: string;
    desc: string;
    icon: string;
    tags: string[];
}

export interface TechItem {
    name: string;
    icon: string;
}

export interface Stat {
    label: string;
    value: string;
}
